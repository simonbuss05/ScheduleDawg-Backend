spring.datasource.url=jdbc:postgresql://localhost:5432/scheduledawg
spring.datasource.username=scheduledawg_user
spring.datasource.password=scheduledawg123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


anthropic.api.key=${ANTHROPIC_API_KEY}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT signing secret (256-bit, base64) and token lifetime (30 days).
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=2592000000
