-- `finals` was part of the original Hibernate-managed schema, but no
-- Final entity/repository/service/controller was ever built on top of it —
-- dead schema left over from an abandoned feature.
DROP TABLE IF EXISTS finals;
